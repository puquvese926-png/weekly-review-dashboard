# 周复盘独立看板协作约定

## 项目定位
- 这是周复盘看板的 2.0 独立版本，放在 `output/weekly-review-dashboard/2.0/` 下单独维护。
- 页面只承载周复盘，不依附旧主站导航，不回写旧 `versions` 页面。
- 2.0 允许改变展示方式、画风和布局，但不能改变既有图表、表格和指标计算口径。

## 2.0 展示方向
- 顶部导航从锚点滚动改为页面切换。
- 总览、生命周期、渠道、内容、站外、声量是独立页面，不再挤在一条长页面里。
- 整体视觉采用全局工作台布局，减少大面积卡片堆叠。
- 顶部增加复盘结束日选择，周期和样本池随之重算。
- 顶部增加主题切换，当前包含墨蓝、粉釉、青瓷、鎏金。

## 你需要先知道的报表逻辑
- 页面以“最近 30 天发布的帖子”为内容池。
- 默认按“当前自然周”对比“上一自然周”看变化，不是按导入批次看变化。
- 头部展示的是复盘的时间上下文，不是操作提示。
- 所有模块都围绕同一批帖子数据展开，筛选后会联动更新概览、趋势、渠道、内容、站外和品牌声量。

## 核心数据口径
- `发布时间` 是必填字段，用来决定帖子是否进入分析池。
- `统计日期` 是必填字段，用来组成每日/每周的累计快照。
- 导入时如果缺 `发布时间`，该行会被跳过，不再回退到 `统计日期`。
- 导入时如果缺 `统计日期`，该行也会被跳过。
- 目前页面里显示的“贴均贡献”，指的是单帖平均对曝光或互动的贡献，用于判断内容效率，不等同于单个帖子绝对规模。
- `总互动` 采用平台累计互动差值近似计算，基于 `点赞 + 评论 + 转发 + 收藏` 的变化值。
- 内容形式只保留 `视频` 和 `图文`，其他值统一置空，避免脏值污染维度分析。
- 平台/渠道名称会做规范化，`KOL / 社媒 / 社群` 是一级范围，`Facebook / Instagram / TikTok / YouTube / Pinterest` 是二级平台。

## 模块展示逻辑
- `本周概览`：看当前周总量、环比、互动率，以及月目标进度。
- `重点发现`：看当前筛选下最值得关注的渠道、内容或效率异常。
- `生命周期`：看帖子按发布时间批次和老化过程中的曝光、互动变化。
- `渠道分布`：看各渠道/平台的曝光、互动、互动率、贡献差、贴均贡献。
- `内容表现`：看不同内容类型和不同渠道组合的表现，强调高曝光、高互动、高效率和低互动四类。
- `站外曝光`：看站外来源和主要站外平台的贡献结构。
- `品牌声量`：看品牌相关词汇、来源和占比变化。

## 交互和使用方式
- 页面默认打开就是总览，不需要额外配置。
- 顶部筛选分两层：先选渠道类型，再选平台。
- `周 / 天` 控制趋势粒度，`总体 / 渠道` 控制分析视角。
- `导入 Excel/CSV` 会用文件数据替换当前内置数据，当前页面里的分析随之重算。
- `恢复内置` 会回到默认 mock 数据。
- 月目标进度条支持直接编辑，保存后会写入本地存储。
- 生命周期图表支持 hover 到横坐标位置查看对应日期的数据，不需要只盯着点看。

## 导入逻辑
- 导入优先读取文件的第一张表。
- 目前支持 `xlsx` 和 `csv`。
- 导入完成后会提示导入结果，以及缺字段跳过的行数。
- 导入不是“增量叠加”，而是一次性替换当前分析源。
- 由于当前页面没有接数据库，导入后数据只保存在当前页面状态和浏览器本地存储里；刷新后如果没再次导入，就回到内置数据。

## 这个模块能直接看出什么
- 本周整体是涨还是跌，主要涨跌来自曝光还是互动。
- 哪个一级渠道在拉动整体，哪个二级平台贡献更高。
- 哪些内容是“高曝光低互动”或“低曝光高效率”。
- 哪些帖子更像早期放量，哪些更像长尾累积。
- 月目标完成到什么程度，当前周距离目标还有多少。
- 导入数据是否存在字段缺失、口径混乱或平台映射错误。

## 工作规则
- 协作时优先改新项目目录，不回写旧周复盘页面，除非明确要求同步。
- 口径变更先更新文档，再改实现。
- 视觉可以继续调，但不要破坏既有口径和交互。
- 不把源数据问题伪装成页面问题；数据缺失要在导入层暴露出来。

## 范围边界
- 本项目当前只覆盖：
  - 核心指标
  - 本周关键判断
  - 生命周期趋势
  - 渠道效率与内容表现
  - 站外曝光
  - 品牌声量
- 不接真实接口。
- 不处理成交、留资、转化等后链路指标。
- 顶部 Tab 目前只保留结构位，不承诺多页签业务逻辑。

## 验收方式
- 新 HTML 能独立打开并完整渲染。
- `周 / 天` 与 `总体 / 渠道` 切换不跳顶、不报错。
- 生命周期区包含 2 张折线图和 4 张环形图。
- 生命周期表格字段顺序与命名符合最新方案。
- 页面主要图元具备 hover 信息。
- 导入文件后，缺 `发布时间` 的行要被跳过，并在状态栏里能看见跳过数量。

## 目录说明
- `index.html`：页面入口
- `styles.css`：页面样式
- `app.js`：页面渲染、口径计算、导入处理
- `review-mock-data.js`：当前内置 mock 数据
- `COLLAB.md`：项目协作约定
- `TASKS.md`：任务板

## 状态流转
- 任务状态统一使用：
  - `Planned`
  - `In Progress`
  - `Ready for Review`
  - `Done`
- 同一时间默认只有一个 `In Progress` 任务。
