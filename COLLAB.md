# 协作约定

## 两角色模型

```
设计师（对话 A）          审计（对话 B）
独立 Claude 会话          独立 Claude 会话

接需求 → 分析 → 设计       读 handoff → 代码审查
→ 拆分任务 → Codex 提示词   → 派 Codex 视觉审查
→ 交付 handoff 文档        → 综合判定 → 审计报告
```

两个角色运行在**独立会话**中，通过 handoff 文档通信。审计看不到设计对话，确保独立验证。

## 触发方式

- 新对话 → 输入「你是设计师」→ AI 自动加载设计师规则
- 新对话 → 输入「你是审计」→ AI 自动加载审计规则

## 关键原则

- **职责分离**：设计 ≠ 审查。同一会话既设计又审查 = 走过场
- **证伪式验收**：审计默认认为有问题，找失败证据
- **可以说不清楚**：不懂装懂比承认不知道更糟糕
- **消除知识诅咒**：每份文档自包含，不假设读者知道上下文

## 数据口径（不变）

- `发布时间` 和 `统计日期` 为必填字段
- 内容池 = 复盘结束日前 30 天发布的帖子
- 当前周 = 复盘结束日倒推 7 天
- 上周 = 当前周期向前平移 7 天
- `总互动` = 点赞 + 评论 + 转发 + 收藏 的变化值
- 内容形式仅保留 `视频` / `图文`

## 项目结构

```
.claude/
├── rules/
│   ├── designer.md          # 设计师角色规则
│   └── auditor.md           # 审计角色规则
├── prompts/
│   ├── codex-implementer-role.md   # Codex Implementer 角色壳
│   └── codex-reviewer-role.md      # Codex Reviewer 角色壳
├── handoffs/                # 设计师交付的 handoff 文档
└── audits/                  # 审计输出的审计报告
```
